'use strict';
(()=>{
const d=window.FRAMESTUDIO_PLAN, $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const key='nexus.framestudio.phase-plan.review.v1';
let stored={reviewed:{},notes:{},theme:'dark'};
try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&typeof x==='object'){stored.reviewed=x.reviewed&&typeof x.reviewed==='object'?x.reviewed:{};stored.notes=x.notes&&typeof x.notes==='object'?x.notes:{};stored.theme=x.theme==='light'?'light':'dark';}}catch{}
let view='overview',tab='tasks';
const descriptions=['Native shell, all approved UI states, themes and safe project files.','Clean video, separate audio/camera and truthful per-frame metadata.','Import, trim, split, join, crop and save non-destructive edits.','Rich text, shapes, layered video, animation and editable audio/effects.','Real exports, portable raw sessions, recovery and release evidence.'];
function save(){try{localStorage.setItem(key,JSON.stringify(stored));$('#save-status').textContent='Draft review notes saved in this browser. Export a copy.';}catch{$('#save-status').textContent='Browser storage unavailable. Export notes before closing.';}}
function notify(s){$('#toast').textContent=s;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),3200);}
function theme(){document.body.classList.toggle('light',stored.theme==='light');$('#theme').textContent=stored.theme==='light'?'Dark theme':'Light theme';}
const matches=(...xs)=>xs.join(' ').toLowerCase().includes($('#search').value.trim().toLowerCase());
function nav(){ $('#phase-nav').innerHTML=d.phases.map(p=>`<button class="nav phase-nav-item ${view===String(p.number)?'active':''}" data-view="${p.number}"><b class="phase-number">${String(p.number).padStart(2,'0')}</b><div>${esc(p.title)}<small>NOT STARTED</small></div></button>`).join('');document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===view));}
function overview(){
const cards=d.phases.filter(p=>matches(p.title,p.scope,p.outcome)).map(p=>`<button class="phase-card" data-view="${p.number}"><span class="num">PHASE ${String(p.number).padStart(2,'0')}</span><h3>${esc(p.title)}</h3><p>${esc(descriptions[p.number-1])}</p><div class="bottom">20 tasks · 15 UI tests · 15 auto tests</div></button>`).join('');
return `<div class="hero"><div><span class="pill good">APPROVED DESIGN</span> <span class="pill">IMPLEMENTATION NOT STARTED</span><h1>Build your studio.<br>One reviewed phase at a time.</h1><p class="lead">A standalone video recorder and editor, with clean source media, editable effects and a testable path from prototype to release.</p><div class="buttons"><button class="button" data-view="1">Review Phase 1 →</button><a class="button secondary" href="00_START_HERE.md">Read the start guide ↗</a></div></div><a href="reference/approved-prototype/index.html" target="_blank" rel="noopener"><img src="reference/approved-prototype/screenshots/editor-1920x1080.png" alt="Approved Nexus.FrameStudio video editor prototype — reference only"></a></div><div class="stats"><div class="stat"><strong>5</strong><span>Sequential approval gates</span></div><div class="stat"><strong>100</strong><span>Implementation tasks</span></div><div class="stat"><strong>150</strong><span>Native test specifications</span></div><div class="stat"><strong>54</strong><span>Approved UI reference states</span></div></div><div class="section-head"><h2>Your development roadmap</h2><span class="pill accent">${Object.values(stored.reviewed).filter(Boolean).length}/100 TASK SCOPES REVIEWED</span></div><div class="phase-cards">${cards||'<div class="empty">No matching phases.</div>'}</div><div class="notice"><strong>Native tests: NOT RUN.</strong> This is a planning package. The browser prototype's historical tests do not prove Windows capture, encoding, device support or recovery. Each phase requires actual evidence and owner approval.</div><div class="two-col"><div class="card"><h3>Record independently. Edit non-destructively.</h3><p>Keep screen video, camera, microphone and PC audio separate. Store synchronized mouse/keyboard events as metadata. Capture-off data is unavailable—not recoverable by toggling an effect.</p></div><div class="card"><h3>Give your agent a precise starting point.</h3><p>Read the master prompt and selected phase prompt, implement that phase, run its tests, then review the UI, produced files and remaining work before approving the next phase.</p></div></div><div class="doc-links"><a href="01_MASTER_AGENT_PROMPT.md">Master prompt ↗</a><a href="docs/CAPTURE_DATA_CONTRACT.md">Capture/data contract ↗</a><a href="docs/ARCHITECTURE.md">Architecture ↗</a><a href="docs/SOURCE_REUSE_REVIEW.md">Recorder/editor-only source review ↗</a><a href="PACKAGE_VALIDATION.md">Package checks ↗</a></div>`;
}
function phase(){const p=d.phases[Number(view)-1];const base='phases/'+p.slug;let body='';
if(tab==='tasks')body=`<p class="muted">Checkboxes mean “reviewed for scope,” not implemented or passed.</p>`+p.tasks.filter(t=>matches(t.id,t.title,t.implementation,t.doneWhen)).map(t=>`<article class="task"><input type="checkbox" data-task="${t.id}" aria-label="Reviewed scope for ${t.id}" ${stored.reviewed[t.id]?'checked':''}><div style="min-width:0;flex:1"><span class="pill">NOT STARTED</span><h3><span class="task-id">${t.id}</span>${esc(t.title)}</h3><p>${esc(t.implementation)}</p><p class="done"><strong>Done when:</strong> ${esc(t.doneWhen)}</p></div></article>`).join('');
if(tab==='manual')body=`<p class="muted">Native UI test specifications. Expand a case for setup, exact steps and expected results.</p>`+p.manualTests.filter(t=>matches(t.id,t.title,t.prerequisites,t.expected)).map(t=>`<details><summary><span class="pill warn">NOT RUN</span>${t.id} · ${esc(t.title)}</summary><div class="case-content"><p><strong>Prerequisites:</strong> ${esc(t.prerequisites)}</p><ol>${t.steps.map(s=>`<li>${esc(s)}</li>`).join('')}</ol><p><strong>Expected:</strong> ${esc(t.expected)}</p><p><strong>Evidence:</strong> <code>${esc(t.evidence)}</code></p><p>Actual result / defect: not supplied.</p></div></details>`).join('');
if(tab==='auto')body=`<p class="muted">Test implementations do not exist yet. These commands apply after the native solution and corresponding tests are created.</p>`+p.automatedTests.filter(t=>matches(t.id,t.title,t.type,t.scenario,t.assertions)).map(t=>`<details><summary><span class="pill warn">NOT RUN</span><span class="pill">${esc(t.type)}</span>${t.id} · ${esc(t.title)}</summary><div class="case-content"><p><strong>Arrange/execute:</strong> ${esc(t.scenario)}</p><p><strong>Assert:</strong> ${esc(t.assertions)}</p><pre>${esc(t.futureCommand)}</pre><p><strong>Evidence:</strong> <code>${esc(t.evidence)}</code></p><p>Use the phase wrapper to reject zero-test runs. Hardware tests need real devices and consent.</p></div></details>`).join('');
if(tab==='handoff')body=`<div class="card"><h3>Review notes for Phase ${p.number}</h3><p>Use this space for scope questions and requested changes. Notes are drafts, not phase approval or test evidence.</p><textarea class="notes" id="phase-notes" data-phase="${p.number}" aria-label="Phase review notes" placeholder="Screen/task ID · requested change · expected outcome…">${esc(stored.notes[p.number]||'')}</textarea><button class="button secondary" data-export>Export review notes</button><div class="doc-links"><a href="${base}/04_HANDOFF_AND_REMAINING.md">Handoff and remaining-work template ↗</a><a href="templates/PHASE_RESULT.json">Machine-readable result template ↗</a><a href="docs/DECISIONS_AND_APPROVALS.md">Decision register ↗</a></div></div><div class="notice"><strong>Approval remains pending.</strong> Review a working native build, actual media outputs, required passing test evidence, defects and exact remaining work. Only the owner/reviewer may approve the next phase.</div>`;
return `<div class="phase-intro"><div class="phase-title"><div><span class="eyebrow">IMPLEMENTATION PHASE ${String(p.number).padStart(2,'0')} / 05</span><h1>${esc(p.title)}</h1></div><span class="pill">NOT STARTED</span></div><p class="lead">${esc(p.outcome)}</p><div class="meta-grid"><div class="card"><h3>In this phase</h3><p>${esc(p.scope)}</p></div><div class="card"><h3>Outside this phase</h3><p>${esc(p.excluded)}</p></div></div><div class="gate"><strong>OWNER APPROVAL GATE</strong>${esc(p.gate)}</div><div class="doc-links"><a href="${base}/AGENT_PROMPT.md">Phase ${p.number} AI-agent prompt ↗</a><a href="${base}/01_IMPLEMENTATION.md">Implementation file ↗</a><a href="${base}/02_MANUAL_TESTS.md">Manual cases ↗</a><a href="${base}/03_AUTOMATED_TESTS.md">Automated cases ↗</a></div></div><div class="tabs">${[['tasks','20 tasks'],['manual','15 manual UI tests'],['auto','15 automated tests'],['handoff','Notes & handoff']].map(([id,label])=>`<button class="tab ${tab===id?'active':''}" data-tab="${id}">${label}</button>`).join('')}</div>${body}`;
}
function features(){return `<span class="eyebrow">REQUIREMENT TRACEABILITY</span><h1>20 editor + 20 recorder points</h1><p class="lead">The approved feature scope is mapped to its primary phase, reference screens and entry acceptance cases. Seven additional approved-screen capabilities remain explicitly included.</p><div class="doc-links"><a href="docs/FEATURES_20_PLUS_20.md">Full feature summary ↗</a><a href="data/plan.json">Machine-readable plan ↗</a></div><table class="coverage"><thead><tr><th>ID</th><th>Requirement</th><th>Phase</th><th>Acceptance pointers</th></tr></thead><tbody>${d.requirements.filter(r=>matches(r.id,r.title,r.manualCase,r.automatedCase)).map(r=>`<tr><td>${r.id}</td><td>${esc(r.title)}<div class="muted">Screens ${r.screens.map(n=>String(n).padStart(2,'0')).join(', ')}</div></td><td><button class="button secondary" data-view="${r.primaryPhase}">P${r.primaryPhase}</button></td><td>${r.manualCase}<br>${r.automatedCase}<br><span class="pill warn">NOT RUN</span></td></tr>`).join('')}</tbody></table>`;}
function screens(){return `<span class="eyebrow">APPROVED PROTOTYPE REFERENCE</span><h1>Every screen has an owner.</h1><p class="lead">Phase 1 builds all 54 native visual states. Each card shows primary functional completion. Reference links open the approved HTML prototype—not a working native recorder.</p><div class="doc-links"><a href="docs/SCREEN_PHASE_MAP.md">Full screen-to-phase map ↗</a><a href="reference/approved-prototype/index.html" target="_blank" rel="noopener">Open prototype ↗</a></div><div class="screen-grid">${d.screens.filter(p=>matches(p.number,p.title,p.group,p.description)).map(p=>`<a class="screen-item" href="${p.path}" target="_blank" rel="noopener"><span class="number">${String(p.number).padStart(2,'0')} / ${esc(p.group)}</span><strong>${esc(p.title)} ↗</strong><p>${esc(p.description)}</p><span class="pill">Visual P1 · Functional P${p.functionalPhase}</span></a>`).join('')}</div>`;}
function render(){nav();$('#content').innerHTML=view==='overview'?overview():view==='features'?features():view==='screens'?screens():phase();}
function exportNotes(){const payload={kind:'nexus.framestudio.plan-review',planVersion:d.version,exportedAt:new Date().toISOString(),scopeReviewedTaskIds:Object.keys(stored.reviewed).filter(k=>stored.reviewed[k]),phaseNotes:stored.notes,nativeImplementationStatus:'NOT_STARTED',nativeTestsStatus:'NOT_RUN',approval:'NOT_GRANTED_BY_THIS_REVIEW_PAGE'};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Nexus.FrameStudio.Plan-Review.json';document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);notify('Review notes exported. Native completion and approvals are unchanged.');}
document.addEventListener('click',e=>{const b=e.target.closest('[data-view],[data-tab],[data-export]');if(!b)return;if(b.hasAttribute('data-view')){view=b.dataset.view;tab='tasks';$('#search').value='';render();window.scrollTo(0,0);}else if(b.hasAttribute('data-tab')){tab=b.dataset.tab;$('#search').value='';render();}else exportNotes();});
document.addEventListener('change',e=>{if(e.target.matches('[data-task]')){stored.reviewed[e.target.dataset.task]=e.target.checked;save();}});
document.addEventListener('input',e=>{if(e.target.matches('#phase-notes')){stored.notes[e.target.dataset.phase]=e.target.value;save();}});
$('#search').addEventListener('input',render);$('#theme').addEventListener('click',()=>{stored.theme=stored.theme==='dark'?'light':'dark';theme();save();});$('#export').addEventListener('click',exportNotes);theme();render();
})();
