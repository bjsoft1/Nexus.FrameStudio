# Approved prototype reference

`approved-prototype/` is an unchanged extraction of the user-approved `Nexus.FrameStudio.Prototype.zip`. Start its `index.html` or use the screen links in this plan. Original historical docs, limitations, synthetic examples and browser test report are preserved as supplied.

A statement inside that reference about phases being deferred describes its original design-review stage. The new five-phase plan is outside the reference folder and reflects the user's subsequent approval. Historical prototype browser passes do not change the current native NOT_RUN status.

The source ZIP for Nexus.ProductivityCare is not included. Its recorder/editor-only reuse boundary is documented in `../docs/SOURCE_REUSE_REVIEW.md`.
